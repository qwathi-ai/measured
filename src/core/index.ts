import a from './autocast'
import h from './helpers'
import o from './options'
import s from './stack'

/**
 * ## Lib
 * Core lib
 */
export namespace lib {
  /** autocast symbol function. */
  export const stack = s
  /** useful helpers. */
  export const helpers = h
  /** options namespace. */
  export const options = o
  /** autocast symbol function. */
  export const autocast = a
  // /** models for measured */
  // export const models = m
}

export default lib
